@model IEnumerable<Praise2017.Controllers.Detail>

@{
    ViewBag.Title = "DetailList";
}
<div class="DetailList">
    <h2>DetailList</h2>

    @*@foreach (var p in Model)
    {
        <div class="item">
            <p><em>@p.Name</em> <i>praised</i> <em>@p.ToWho</em> for</p>
            <p>@p.Descrip</p>
            <p>@p.UpdateDate</p>
        </div>
    }*@

    <dl>
    @foreach (var p in Model)
    {
        <dt><em>@p.Name</em> <i>praised</i> <em>@p.ToWho</em> for</dt>
        <dd>@p.Descrip
            <p class="text-right text-muted">@p.UpdateDate</p>
        
        </dd>
    }
       
    </dl>
</div>



